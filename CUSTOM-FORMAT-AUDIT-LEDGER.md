# Custom-format audit-and-fix loop ledger

started_at: 2026-08-06T22:11:09Z
status: active
consecutive_dry: 1
iteration: 7

Repo: fork `4eh5xitv6787h645ebv/jakes-profilarr-database`, branch `fix/regex-audit`.
Safety: the fork's live v1 `stable` and `custom` branches are read-only for this loop. Never write to any upstream repository.
Scope: custom-format condition wiring, required/optional/negated semantics, Radarr/Sonarr scoping, score reachability, parser behavior, tests, and v1/v2 translation drift. Regex text is in scope only when needed to evaluate a custom-format graph; settled regex hypotheses in `LEDGER.md` remain closed.
Harness: `audit/harness/`, with the schema clone at `~/work/dictionarry-fix-loop/schema`.
Optional local controls: `/home/jake/media-tests-video-files/`, documented in `audit/LOCAL-TEST-ASSETS.md`; repository tests must not depend on this local-only path.
Stop rule inherited from the completed regex loop: stop after five consecutive dry iterations or 40 hours from `started_at`, whichever comes first.
Publication rule: every landed migration gets a newest-first card in `audit/regex-fixes.html`, and `docs/index.html` must be updated in the same commit and remain byte-identical. The user's Claude artifact is deferred by request.
Commit rule: every migration, harness change, corpus/test addition, report update, and ledger update is committed and pushed separately to the safe fork immediately.

## Baseline

- Fresh replay: 228 base operations (ops 0–227) plus `tweaks/hdr-dv-truth-corpus.sql`; foreign keys clean and `PRAGMA integrity_check = ok`.
- Current graph: 257 custom formats, 1,646 conditions, 1,216 condition-pattern links, 2,227 profile-score rows, 206 parser tests, 11 quality profiles, and 533 regex definitions.
- Condition types: 818 release-group, 398 release-title, 258 source, 155 resolution, seven indexer-flag, four release-type, four language, and two quality-modifier rows.
- Condition flags: 987 optional-positive, 416 required-positive, 243 required-negated, and zero optional-negated rows.

## Settled custom-format verdicts — do not repeat without new graph evidence

- **DUBBED wiring**: fixed by op 217; its mutually exclusive Radarr/Sonarr title conditions are optional and OR correctly. The format remains deliberately unscored.
- **Extras wiring and profile coverage**: fixed by ops 218 and 231. Its mutually exclusive app-scoped title conditions are optional and OR correctly, and every one of the 11 profiles now carries the −999999 score for both apps. Compact's prior omission came from the testing-only v1 refactor `c156dfad`, not an intentional policy exception; do not remove either Compact score without new explicit policy evidence.
- **QxR/TAoE/HONE-style title-positive plus group-negated fallbacks** are deliberate complements to parsed-group tiers, not inverted conditions. Dashless member suffixes remain a parser/tier-policy issue.
- **Dolby Vision (Without Fallback), HDR/SDR, Atmos (Missing), DTS, DTS-HD MA, Special/Extended Edition, Full Disc, and German DL** graphs are covered by the completed regex loop's compound gates; reopen only if this audit finds a new structural or scoring path.
- **HDR10 (Missing) on Sonarr** is explicitly documented as incompatible with its required source design and is unscored; do not silently turn it into a new policy.
- The prior full title-regex conjunction sweep found no other mutually exclusive required title pair after ops 217–218. This loop will still audit different axes: source/resolution compatibility, app scoping, score reachability, stale/deleted wiring, and parser-test coverage.
- **Score fallback precedence**: an `arr_type=all` score is intentionally shadowed by an app-specific score for the same profile/format/app. Profilarr selects both and the app-specific row overrides the fallback; the 72 fully shadowed triples in the quality-tier families are cleanup debt, not double scoring.
- **App filtering**: Profilarr filters non-target conditions plus Radarr-only quality modifiers and Sonarr-only release types before evaluation. No scored format has zero conditions for either effective target app.
- **EA tier membership**: fixed by op 229. The authoritative v1 move commit `a0a23ed` places EA in `720p Quality Tier 5`; its older Tier 4 membership was stale residue. Keep EA only in Tier 5 unless new policy evidence supersedes that history.
- **Remux detector union**: fixed by op 230. Keep the base title detector and the Radarr `quality_modifier=remux` / Sonarr `source=bluray_raw` fallbacks as mutually exclusive branches under a shared Not-DVD policy: `notDVD AND (title OR appClassifier)`. Do not merge different implementation types into one format to express OR, and do not restore literal v1 double scoring.
- **Golden Popcorn app scope**: no database migration is safe or currently needed. All three definitions have zero score rows after final-v1 commits explicitly removed their Radarr-only scores. Their `ptp_golden` value is Radarr-only, but scoping only that condition would leave dangerously broad Sonarr resolution/group formats; scoping every condition would compile an empty/rejected format and Profilarr's evaluator treats an empty graph as vacuously true. Reopen only with format-level app scope or a sync preflight that omits the whole format for the wrong app; do not restore historical scores as an audit side effect.
- **WEBRip app divergence** is explicit policy, not condition loss. Ops 3/7 scope quality-tier WEBRip eligibility to Radarr, op 36 removes Sonarr WEBRip source scores, and op 43 deliberately makes `Banned WEBRip` evaluate as source-only on Sonarr under the operation name “Ban All WEBRips on non 265 Profiles Sonarr Side.”
- **Largest app-score differences** are explicit/compositional. Efficient Radarr uses a separate 700000-point Bluray base and op 145 deliberately lowers only its Balanced-tier additions to 181000/180000; Sonarr uses standalone 861000/860000 tiers. Op 22 explicitly sets `720p Quality` SDTV to 20000 for Radarr and 200000 for Sonarr. Do not normalize these values merely because they differ.

## Open hypotheses

- Description-only debt is tracked separately from runtime bugs: SDR says “Ban” despite deliberate score-zero highlighting; Remux claims an h264/h265 test it does not have; TrueHD (Missing), HDR10 (Missing), Banned WEBRip, Audio Description, and 2160p Quality Tier 6 also have stale or incomplete descriptions.

## Searched sources and queries

- Inherit the exhausted source/query pairs in `LEDGER.md`; do not repeat them.
- Profilarr score transformer and condition evaluator: app-specific score precedence, target-app filtering, and required/optional type-group behavior.
- Fresh whole-graph SQL scans: backing cardinality/type, exact and semantic duplicate signatures, mutually exclusive required values, app expansion, effective scores, scoreless formats, test coverage, and sequential graph history across ops 0–227.
- v1 final YAML on the fork's read-only `stable`/`custom` branches: `720p HDTV Tier 3` carries the same erroneous 1080p condition; the initial v2 translation preserved it exactly.
- Fresh metadata-only Prowlarr query `HANDJOB HDTV`: one real victim, `ESPN.E60.WWE.Behind.The.Curtain.720p.HDTV.x264-HANDJOB`. No content was downloaded.
- Profilarr parser `/parse` and `/match/batch`: the real title parses as television, 720p, release group HANDJOB, with both title/group regexes matching; the constructed 1080p twin parses identically except for resolution.
- EA history across v1 commits `99eb353`, `cf370415`, and `a0a23ed` plus merged PR 93: EA began in 720p Tier 4, was later added to Tier 6, then was explicitly moved to 1080p/720p Tier 5 without removing the old 720p Tier 4 row. The same PR's `fb05d23` move removes its source-tier member, and the neighboring 1080p family ends with EA only in Tier 5. Initial v2 op 0 copied the stale overlap; no later operation changed it.
- Independent TRaSH Guides history carries EA in exactly one HD Bluray tier. Current whole-graph replay likewise found EA to be the only duplicated release group across the base `* Quality Tier N` families.
- Fresh metadata-only Prowlarr controls: movie `Legiony.2019.720p.BluRay.DD5.1.x264-EA` and series `My.Hero.Academia.S03E24.720p.BluRay.AAC.2.0.x264-EA`; both parse as Bluray, 720p, release group EA. The bundled `1883.S01.720p.BluRay.DD5.1.x264-NTb` control parses into Tier 4 only. No content was downloaded. The optional local 12,464-file media corpus and local Arr history contained no exact EA release-group control.
- Remux history across v1 commits `99eb353`, `8b164ac`, `0803d3f`, `7739c74`, `8de882a`, and `4cb8e86`: final v1 deliberately kept separate title, Radarr quality-match, and Sonarr raw-source custom formats. Initial v2 op 0 preserved all three, then ops 9–11 deleted the two app-native formats and merged their conditions into the title format.
- Native Radarr/Sonarr source (`CustomFormatCalculationService.cs` and `SpecificationMatchesGroup.cs`) and Profilarr's evaluator confirm the decisive semantics: implementation-type groups are ANDed; within a type, required conditions must all pass and optional siblings supply an any-match only when that group has no required member. The merged v2 formula was therefore Radarr `title AND notDVD AND modifier` and Sonarr `title AND notDVD`, with the optional raw-source sibling ignored.
- The full 1,734-title bundled sweep found exactly two Radarr gains from the repaired union and zero losses: `AVATAR 2009 4K REMASTER REMUX [RoB]` exercises the title-only arm, while `The Dark Knight (2008) UHD MKVRemux 2160p DTS-HD NL` exercises the parser-only arm. The mutual-exclusion truth table has zero union differences and zero overlaps in all eight title/classifier/DVD states.
- Fresh metadata-only Prowlarr/native-Radarr controls prove both live escape classes: `A.White.Spot.in.the.Back.of.the.Head.1979.720p.BluRay.Remux.DD.2.0-PTP` parses with modifier None despite its Remux title, while `Ricky.Bobby.Koenig.Der.Rennfahrer.2006.THEATRICAL.GERMAN.DL.1080P.BLURAY.AVC-UNDERTAKERS` parses as Remux-1080p despite lacking a Remux title token. DVD and DVD-R controls motivated retaining Not DVD as a global policy gate. No content was downloaded.
- Profilarr's parser cannot faithfully represent Sonarr `bluray_raw`, and it disagrees with native Radarr on the decisive 720p/German classifier cases. The tracked parser suite therefore uses the two bundled Radarr fixtures where both parsers agree, while the Sonarr source arm is gated by exact graph assertions, native Arr semantics, and the exhaustive Boolean proof rather than a knowingly false parser expectation.
- Golden Popcorn history: `e94f364` created the 1080p/720p/SD formats, `f9968b0` corrected SD resolution logic, and `d1f88f1` added the release-group negations and briefly rescored the flags. After the profile app split, every score lived only under `custom_formats_radarr`; `25bf4c4` removed SD scores and `bb5e8d2` removed all remaining 1080p/720p scores as unsupported. Initial v2 commit `00bd245` correctly carried no scores but defaulted all ten conditions to `arr_type=all`; later descriptions explicitly say “connected directly to Radarr.”
- Native enums and Profilarr's transformer prove why a condition-only Golden Popcorn patch is unsafe. Radarr defines `PTP_Golden=8`; Sonarr uses value 8 for `Internal` and has no PTP flag. Profilarr deliberately maps `ptp_golden` to Radarr 8 and Sonarr 0, which Sonarr rejects. Filtering only the flag emits a resolution/group-only Sonarr format; filtering all ten conditions emits no specifications, also rejected by Arr's NotEmpty validator. With zero score references, normal quality-profile sync never reaches any of the three formats.
- No valid Golden Popcorn positive fixture exists locally: all 1,734 bundled releases lack a Golden flag, local Prowlarr has no direct PTP indexer, and local Radarr files/history contain no bit-8 record. Profilarr's current test evaluator reports indexer flags as unavailable and does not model app scope, so adding a title-only test would be misleading. Real bundled 1080p, 720p, and 480p non-Golden controls demonstrate how a flag-only scope change would broaden the three Sonarr graphs.
- Extras history: `359c5ff` split the Radarr `Extras` and Sonarr `TV Extras` hard bans, and `bab2e7c` created Compact's direct profile ancestor with both. Renames `ce1d15f`/`65b51ad` retained them. Commit `c156dfad` removed both during the broad “Refactor Source and Release Group Tiers Compact” rewrite whose body says “DO NOT USE COMPACT PROFILE TESTING ONLY”; it retained neighboring 3D, Full Disc, Remux, and Sing Along bans and gave no Extras-policy rationale. `8cebf8c` later removed the testing warning without restoring the rows. Initial v2 `00bd245` and merge op 4 faithfully inherited the omission.
- Current score-policy scan: Extras was the only near-universal hard ban at 20/22 effective profile/app slots, with Compact/Radarr and Compact/Sonarr the only gaps. All ten peers had exact −999999 rows, and all 11 profiles use a 200000 minimum. Across 1,734 bundled releases, 40 match Extras and ten cleared Compact's floor without the ban; zero clear it after the restored score. The highest affected release falls from 924610 to −75389.
- Paired bundled controls prove the live outcome without changing any release parser behavior: Avengers Digital Extras falls from 863660 to −136339 while its main feature stays 863660; The Last of Us S01 Extras falls from 862510 to −137489 while its main season stays 863110. Fresh metadata-only Prowlarr positives and the six-positive/two-negative tracked Extras tests confirm both app branches and preserve show/movie-title controls. The optional local media fixture `E001 Complete Extras Layout (2026)-extra.mkv` also matches, but local files remain supplementary and are not a repository dependency.
- Post-op231 structural sweep: all 1,649 conditions have exactly one correctly typed backing row; no effective app/type group mixes required and optional conditions or contains contradictory required scalar values. All 2,776 effective scored profile/format/app slots retain at least one condition after app filtering. Thirty semantic duplicate graph pairs are Compact/Efficient aliases or separated banned-group aliases, and none share a scored profile/app slot. The two post-filter empty graphs are unscored app-incompatible formats (`Full Disc (Quality Match)` on Sonarr and `Season Pack` on Radarr).
- Score/test reachability sweep: the only `all` plus app-specific score overlaps are the 72 previously settled shadowed triples (144 override rows), and no near-universal hard-ban hole remains. Of 231 scored formats, 21 have direct parser tests; the 1,734 bundled releases exercise 178 scored formats. The remaining coverage deficit is harness debt, but every exercised resolution/source family agreed with parser fields and no same-family tier stack reached a shared score scope.

## Iteration log

- **Iteration 1 (2026-08-07)**: area = whole-database structural inventory, score reachability, v1/v2 translation, and parser-test coverage. Result: **1 live bug fixed (op 228)**. `720p HDTV Tier 3` required 1080p, making its full graph identical to `1080p HDTV Tier 3`; both formats overlap in all 11 profiles for both apps, so 1080p HANDJOB HDTV could stack +60000 while the intended 720p release missed +20000. Op 228 exact-guards both the condition name and backing resolution to 720p. Fresh replay of 229 ops plus the tweak is FK-clean with integrity `ok`; all 44 score rows are unchanged; 588/588 native regex checks and 210/210 Profilarr parser-backed tests pass. Profilarr validation database 2 synced through commit `100164e` in job 17 (229 base ops + one tweak). The report and `docs/index.html` were updated byte-identically (SHA-256 `7db189f4798fffaf29da7a9a0c3b8a682be82ca4840c1b9216f5178b0dd09aa7`) and pushed in `0266103`. The bug exists in both v1 and v2; the fork's live v1 branches were inspected read-only and not modified. `consecutive_dry` remains 0; iteration advances to 2.
- **Iteration 2 (2026-08-07)**: area = duplicated release-group tier membership and the complete EA score/app graph. Result: **1 live bug fixed (op 229)**. `720p Quality Tier 4` retained stale EA membership after authoritative commit `a0a23ed` moved EA to Tier 5. The two formats otherwise have identical effective gates and are live at +142000 and +141000 in all 11 profiles for both apps, so eligible Bluray EA releases stacked +283000 in every one of 22 profile/app slots. Op 229 exact-guards and removes only the stale Tier 4 EA condition; its repeat is a no-op, the Tier 5 condition remains exactly once, all score rows are unchanged, foreign keys are clean, and integrity is `ok`. Fresh replay covers 230 base ops plus the tweak; 588/588 native regex checks and 216/216 Profilarr parser-backed tests pass. Direct entity evaluation proves real EA movie and series titles match Tier 5 only, while the real NTb control remains Tier 4 only. Migration `97b4375`, test commits `07d333a` and `bae6ab7`, and Profilarr sync job 19 were pushed to the safe fork. The report and `docs/index.html` were updated byte-identically (SHA-256 `870d0435cdff6adc65751e2e6057def022de58ead9ac70947778e99bd0ad6742`) and pushed in `716f84e`. The bug exists in both v1 and v2; all comparison branches were read-only. `consecutive_dry` remains 0; iteration advances to 3.
- **Iteration 3 (2026-08-07)**: area = v1-to-v2 Remux merge history, native Arr condition-group semantics, app-specific score reachability, and DVD policy. Result: **1 live bug fixed (op 230)**. V2 ops 9–11 converted the intended title-or-parser detector union into a Radarr intersection and discarded Sonarr's source-only fallback, allowing real Remux releases to escape −999999 in the same nine profiles per app. Op 230 restores `Remux (Quality Match)` for Radarr and `Remux (Source)` for Sonarr, gives both a required negated link to the shared title regex, applies Not DVD to every branch, and restores 18 app-specific score rows. The resulting disjoint formula is `notDVD AND (title OR appClassifier)`: it recovers both lost arms while every ordinary Remux receives exactly one −999999 instead of final v1's possible −1999998 stack. Fresh replay covers 231 base ops plus the tweak and yields 259 formats, 1,649 conditions, 1,217 pattern links, 2,245 score rows, and 221 tests; repeat application is a no-op, foreign keys are clean, and integrity is `ok`. All 588/588 .NET checks and 221/221 Profilarr parser/evaluator tests pass; validation database 2 synced commit `7e68158` in job 20. Migration `78cc27d` and test commit `7e68158` were pushed to the safe fork. The report and `docs/index.html` were updated byte-identically (SHA-256 `13660d37b64dc45da49ba1c64a6cb8b6a5fd42b9c1cd7d553df4350f57c72ec4`) and pushed in `5b11fa5`. This is a v2-only regression; v1 history and every comparison repository were inspected read-only. `consecutive_dry` remains 0; iteration advances to 4.
- **Iteration 4 (2026-08-07)**: area = Golden Popcorn history, app scope, native indexer-flag enums, transformer output, and score reachability. Result: **dry; no migration**. The three formats are intentionally dormant with zero score rows after explicit final-v1 removals, so no shipped Radarr or Sonarr profile sync reaches them. Their `arr_type=all` PTP flag is a real translation mismatch, but the schema has no format-level app scope: changing only the flag to Radarr strips the decisive condition in Sonarr and creates a broad false-positive format, while changing the whole graph to Radarr yields an empty/rejected Sonarr custom format and a vacuously true local evaluation. Restoring scores would reverse explicit policy history and is out of scope. The latent mismatch is settled pending runtime/schema support; no files beyond this ledger and no Arr configuration were changed. `consecutive_dry` advances to 1; iteration advances to 5.
- **Iteration 5 (2026-08-07)**: area = Extras score reachability, Compact profile history, app-specific detector graph, bundled release outcomes, and universal hard-ban coverage. Result: **1 live bug fixed (op 231)**. Compact alone lacked the Radarr and Sonarr Extras scores after the explicitly testing-only v1 refactor `c156dfad` removed both without policy rationale; initial v2 preserved the inherited hole. Op 231 exact-guards the repaired two-condition graph and the ten-profile peer matrix, then restores only Compact's two −999999 rows. Fresh replay covers 232 base ops plus the tweak and yields 259 formats, 1,649 conditions, 1,217 pattern links, 2,247 score rows, and 221 tests; repeat application is a no-op, foreign keys are clean, and integrity is `ok`. The new database-level invariant fails on the old 20-slot state and passes the complete 22-slot matrix. All 588/588 .NET checks and 221/221 Profilarr parser/evaluator tests pass; validation database 2 synced commit `c8f7015` in job 21. Migration `7a8c03f` and harness commit `c8f7015` were pushed to the safe fork. The report and `docs/index.html` were updated byte-identically (SHA-256 `3480220acee45f2d6f7f267a770d912b801f4174ce1234bb00150f0a59d5b459`) and pushed in `f3a46c1`. The bug exists in both v1 and v2; every comparison branch was inspected read-only. `consecutive_dry` resets to 0; iteration advances to 6.
- **Iteration 6 (2026-08-07)**: area = complete post-fix structural invariants, semantic graph duplication, effective app filtering, score-shadow/coverage anomalies, and parser/bundled-release reachability. Result: **dry; no migration**. All condition backing, effective graphs, required/optional groups, and duplicate-score intersections are clean. The strongest apparent app broadening, Sonarr's source-only `Banned WEBRip`, is the exact policy named by op 43 after ops 3/7/36 removed Sonarr WEBRip tier eligibility. The two largest score asymmetries are likewise explicit: op 145 makes Efficient Radarr tier scores compositional with a separate source base, and op 22 gives Sonarr SDTV a standalone floor-reaching score. The bundled sweep found no resolution/source mismatch, unintended same-family stacking, or cross-app reachable movie/TV tier. No repository files other than this ledger and no Arr configuration changed. `consecutive_dry` advances to 1; iteration advances to 7.
